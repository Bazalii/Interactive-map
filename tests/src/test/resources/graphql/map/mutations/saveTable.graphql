mutation saveTable($tableDto: TableDtoInput!) {
    saveTable(tableDto: $tableDto) {
        coordinateX
        coordinateY
        id
        creator
        length
        width
        color
    }
}