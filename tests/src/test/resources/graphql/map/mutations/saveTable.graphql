mutation saveTable($tableRequest: TableRequestInput!) {
    saveTable(tableRequest: $tableRequest) {
        coordinateX
        coordinateY
        id
        creator
        length
        width
        color
    }
}